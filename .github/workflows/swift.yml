name: Swift

on:
  push:
    branches:    
      - master

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: |
        sudo xcode-select --switch /Applications/Xcode_11.1.app
        xcodebuild -version
        swift --version
        swift build -v
    - name: Run tests
      run: swift test -v
