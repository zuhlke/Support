name: Swift

on:
  push:
    branches:    
      - master

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Prepare Xcode
      run: |
        sudo xcode-select --switch /Applications/Xcode_11.1.app
        xcodebuild -version
        swift --version
    - name: Boot simulator
      run: simctl boot "iPhone 11 Pro"
    - name: Run tests
      run: xcodebuild test -workspace .swiftpm/xcode/package.xcworkspace -scheme Support-Package -destination "name=iPhone 11 Pro"
